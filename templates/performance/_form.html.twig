{% form_theme form 'bootstrap_4_layout.html.twig' %}
<div class="text-white">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn text-white">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>